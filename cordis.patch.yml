# dsh-plugin-browser-harness: agent-driven local Chrome control via CDP.
# Single bundle row: tools + browser-connection prompt + HTTP API (registered
# only when a webServer exists) + the 设置 > 浏览器连接 settings page and the
# 设置 > 插件 > 插件配置 settings card.
- insert:
    - id: browser-harness
      name: 'dsh-plugin-browser-harness'
